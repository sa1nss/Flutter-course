name: lab4
description: Resume builder - lab4 (modified to include GitHub stats via provider)
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^8.0.0
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  http: ^0.13.5
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Якщо у тебе є ресурси (зображення, шрифти), додай їх сюди
  # assets:
  #   - assets/images/
  #
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
